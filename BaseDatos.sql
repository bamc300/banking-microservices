-- Base de datos para clientes_db
\c clientes_db;

-- Tabla de clientes (se crea automáticamente con JPA)
-- La estructura se genera automáticamente por Hibernate/JPA

-- Base de datos para cuentas_db  
\c cuentas_db;

-- Tabla de cuentas (se crea automáticamente con JPA)
-- La estructura se genera automáticamente por Hibernate/JPA

-- Tabla de movimientos (se crea automáticamente con JPA)
-- La estructura se genera automáticamente por Hibernate/JPA